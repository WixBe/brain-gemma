# React Frontend — Local Environment
# Copy to .env.local (gitignored by Vite by default)

# API base URL — point to the FastAPI server
VITE_API_BASE_URL=http://localhost:8000

# Set to 'false' to disable mock mode and call the real API
VITE_MOCK_MODE=true
